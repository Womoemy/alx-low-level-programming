#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @int: integer whose absolute value is to be computed
 *
 * Return: Always 0 (success)
 */
int _abs(int c)
{
	_abs(c);

	return (0);
}
